server:
  port: 8081
jpa:
  hibernate:
    ddl-auto: create

eureka:
  client:
    serviceUrl:
      defaultZone: "http://localhost:8070/eureka"
  instance:
    prefer-ip-address: true

management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"
  
